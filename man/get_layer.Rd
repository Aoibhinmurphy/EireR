% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_layer.R
\name{get_layer}
\alias{get_layer}
\title{Download any dataset from the EireR registry}
\usage{
get_layer(dataset_id, quiet = FALSE)
}
\arguments{
\item{dataset_id}{A dataset ID from `eire_datasets$dataset_id`.}

\item{quiet}{Suppress messages. Default `FALSE`.}
}
\value{
An `sf` object in EPSG:2157 (ITM).
}
\description{
Provide any `dataset_id` from
`eire_catalogue()`, downloads and returns the layer as an sf object
in ITM (EPSG:2157).
}
\details{
This unlocks all datasets in the registry with a single function.
Use `eire_catalogue()` to browse available datasets and find their IDs.
}
\examples{
\dontrun{
# Browse available datasets
eire_catalogue(theme = "hydrology")

# Download a specific layer by ID
rivers <- get_layer("epa_wfd_riverwaterbodies_cycle3")
catchments <- get_layer("epa_water_riverbasins")

# Plot it
plot_eire(rivers)
}
}
